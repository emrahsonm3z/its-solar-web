import options from "constants/chartOptions/gauge";

const initialSettings = options;

export default function(state = initialSettings, action) {
  switch (action.type) {
    default:
      return state;
  }
}
